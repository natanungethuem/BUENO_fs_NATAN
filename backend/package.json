{
  "name": "ecore-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install:node": "rm -rf ./node_modules package-lock.json && npm i",
    "install:python": "pip install -r backend/requirements.txt.",
    "lint:python": "find ./backend -name '*.py' | xargs pylint",
    "lint:python:fix": "cd ./backend && autopep8 --in-place --aggressive --aggressive --recursive *.py"
  },
  "author": "",
  "license": "ISC"
}
